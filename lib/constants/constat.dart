import 'package:flutter/material.dart';

const swidth = SizedBox(width: 10);
const sheight = SizedBox(height: 10);

//Moviecard border radius

const BorderRadius cardRadius10 = BorderRadius.all(Radius.circular(10));
const BorderRadius cardRadius20 = BorderRadius.all(Radius.circular(20));
const BorderRadius cardRadius30 = BorderRadius.all(Radius.circular(30));
