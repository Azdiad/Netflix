import 'package:flutter/material.dart';

class treandingMovies extends StatelessWidget {
  const treandingMovies({super.key, required this.treanding});
  final List treanding;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
